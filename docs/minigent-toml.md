# Minigent TOML compatibility

The unified configuration reference moved to [`mindweft-toml.md`](mindweft-toml.md).
Legacy `minigent.toml` files and `MINIGENT_*` environment variables remain supported during the
Mindweft migration.
